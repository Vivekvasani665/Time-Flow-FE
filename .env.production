# Loaded by `next build` in production. The backend URL is baked into the /api
# rewrites at build time; a value set in the hosting dashboard takes precedence.
BACKEND_INTERNAL_URL=https://time-flow-be-yznc.onrender.com
